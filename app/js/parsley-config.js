const ParsleyConfig = window.ParsleyConfig = {
  trigger: 'change',
  successClass: 'has-success',
  errorClass: 'has-error',
  errorsWrapper: `<ul class="errors"></ul>`,
  errorTemplate: `<li class="errors__item"></li>`
}

export default ParsleyConfig
